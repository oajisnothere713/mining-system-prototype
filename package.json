{
  "name": "mining-system-prototype",
  "version": "1.0.0",
  "description": "Field Ops Intelligence — Full-stack MERN application for mining operations management",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"bun run server\" \"bun run client\"",
    "server": "cd server && bun run dev",
    "client": "cd client && bun run dev",
    "install-all": "bun install && cd server && bun install && cd ../client && bun install",
    "seed": "cd server && bun run seed",
    "seed:fresh": "cd server && bun run seed:fresh"
  },
  "dependencies": {
    "concurrently": "^9.1.2",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-validator": "^7.3.2",
    "mongoose": "^9.7.0",
    "morgan": "^1.11.0"
  }
}
